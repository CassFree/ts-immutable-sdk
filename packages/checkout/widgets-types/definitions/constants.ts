export enum Network {
  ETHEREUM = 'Ethereum',
  GOERLI = 'Goerli',
  POLYGON = 'Polygon',
}

export enum WidgetTheme {
  LIGHT = "light",
  DARK = "dark",
  CUSTOM = "custom",
}

export enum ConnectionProviders {
  METAMASK = "metamask"
}